# 测试覆盖率改进计划

## 📊 当前状态分析

### 覆盖率现状

- **整体覆盖率**: 20.8% (目标: 50%+)
- **语句覆盖率**: 20.8%
- **分支覆盖率**: 69.46%
- **函数覆盖率**: 26.01%
- **行覆盖率**: 20.8%

### 已有良好覆盖率的模块

- ✅ **editorStore.ts**: 97.93%
- ✅ **Canvas.tsx**: 90.39%
- ✅ **useImageProcessing.ts**: 74.41%
- ✅ **historyManager.ts**: 73.26%

### 需要改进的模块

- ❌ **UI组件**: Header, Footer, Toolbar, ParamsPanel等 (0%覆盖率)
- ❌ **核心工具**: imageWorker.ts, imageCollageUtils.ts (0%覆盖率)
- ❌ **主应用**: App.tsx, main.tsx (0%覆盖率)
- ❌ **工具函数**: filters.ts, imageUtils.ts (低覆盖率)

## 🎯 改进计划

### 阶段一：UI组件测试 (优先级: 高)

#### 1.1 Header组件测试

**目标文件**: `src/components/layout/Header.tsx`
**测试文件**: `src/components/layout/__tests__/Header.test.tsx`

**测试重点**:

- 组件正常渲染
- 品牌logo显示
- 导航菜单功能
- 响应式布局
- 主题切换功能

**示例测试结构**:

```typescript
describe('Header组件', () => {
  it('应该正确渲染Header组件', () => {
    render(<Header />);
    expect(screen.getByRole('banner')).toBeInTheDocument();
  });

  it('应该显示品牌logo', () => {
    render(<Header />);
    expect(screen.getByAltText(/logo/i)).toBeInTheDocument();
  });

  it('应该响应主题切换', async () => {
    const user = userEvent.setup();
    render(<Header />);

    const themeToggle = screen.getByRole('button', { name: /theme/i });
    await user.click(themeToggle);

    // 验证主题切换逻辑
  });
});
```

#### 1.2 Footer组件测试

**目标文件**: `src/components/layout/Footer.tsx`
**测试文件**: `src/components/layout/__tests__/Footer.test.tsx`

**测试重点**:

- 组件渲染
- 版权信息显示
- 外部链接功能
- 社交媒体链接

#### 1.3 Toolbar组件测试

**目标文件**: `src/components/layout/Toolbar.tsx`
**测试文件**: `src/components/layout/__tests__/Toolbar.test.tsx`

**测试重点**:

- 工具按钮渲染
- 工具选择状态
- 快捷键支持
- 工具切换交互
- 禁用状态处理

#### 1.4 ParamsPanel组件测试

**目标文件**: `src/components/panels/ParamsPanel.tsx`
**测试文件**: `src/components/panels/__tests__/ParamsPanel.test.tsx`

**测试重点**:

- 参数面板渲染
- 参数值变更
- 实时预览更新
- 参数验证
- 重置功能

**预期覆盖率提升**: +15%

### 阶段二：核心业务逻辑测试 (优先级: 高)

#### 2.1 imageWorker.ts测试

**目标文件**: `src/utils/imageWorker.ts`
**测试文件**: `src/utils/__tests__/imageWorker.test.ts`

**测试重点**:

- Worker初始化
- 消息通信机制
- 图像处理操作
- 错误处理
- 性能监控

**测试策略**:

```typescript
describe('ImageWorker', () => {
  let mockWorker: Worker;

  beforeEach(() => {
    // Mock Worker API
    mockWorker = {
      postMessage: vi.fn(),
      terminate: vi.fn(),
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
    } as any;

    // Mock Worker constructor
    global.Worker = vi.fn(() => mockWorker);
  });

  it('应该正确初始化Worker', () => {
    const worker = new ImageWorker();
    expect(global.Worker).toHaveBeenCalled();
  });

  it('应该正确处理图像模糊操作', async () => {
    const worker = new ImageWorker();
    const mockImageData = createMockImageData(100, 100);

    const result = await worker.processImage('blur', {
      imageData: mockImageData,
      params: { intensity: 5 },
    });

    expect(mockWorker.postMessage).toHaveBeenCalledWith({
      type: 'process',
      operation: 'blur',
      data: mockImageData,
      params: { intensity: 5 },
    });
  });
});
```

#### 2.2 imageCollageUtils.ts测试

**目标文件**: `src/utils/imageCollageUtils.ts`
**测试文件**: `src/utils/__tests__/imageCollageUtils.test.ts`

**测试重点**:

- 图像拼贴算法
- 布局计算
- 图像缩放处理
- 边距和间距处理
- 导出功能

**预期覆盖率提升**: +20%

### 阶段三：主应用集成测试 (优先级: 中)

#### 3.1 App.tsx集成测试

**目标文件**: `src/App.tsx`
**测试文件**: `src/__tests__/App.integration.test.tsx`

**测试重点**:

- 应用初始化
- 路由功能
- 全局状态管理
- 错误边界处理
- 完整用户流程

**集成测试示例**:

```typescript
describe('App集成测试', () => {
  it('应该完整加载应用', async () => {
    render(<App />);

    // 验证主要组件都已渲染
    expect(screen.getByRole('banner')).toBeInTheDocument(); // Header
    expect(screen.getByRole('main')).toBeInTheDocument(); // 主内容区
    expect(screen.getByRole('contentinfo')).toBeInTheDocument(); // Footer
  });

  it('应该支持完整的图像处理流程', async () => {
    const user = userEvent.setup();
    render(<App />);

    // 1. 上传图像
    const fileInput = screen.getByLabelText(/upload/i);
    const file = new File(['dummy'], 'test.jpg', { type: 'image/jpeg' });
    await user.upload(fileInput, file);

    // 2. 选择滤镜
    const blurButton = screen.getByRole('button', { name: /blur/i });
    await user.click(blurButton);

    // 3. 调整参数
    const intensitySlider = screen.getByRole('slider', { name: /intensity/i });
    await user.clear(intensitySlider);
    await user.type(intensitySlider, '5');

    // 4. 应用效果
    const applyButton = screen.getByRole('button', { name: /apply/i });
    await user.click(applyButton);

    // 5. 验证结果
    await waitFor(() => {
      expect(screen.getByText(/processing complete/i)).toBeInTheDocument();
    });
  });
});
```

**预期覆盖率提升**: +8%

### 阶段四：工具函数测试完善 (优先级: 中)

#### 4.1 filters.ts测试完善

**目标文件**: `src/utils/filters.ts`
**测试文件**: `src/utils/__tests__/filters.test.ts`

**当前覆盖率**: 21.8%
**目标覆盖率**: 80%+

**测试重点**:

- 所有滤镜算法
- 参数边界值处理
- 性能测试
- 错误处理

#### 4.2 imageUtils.ts测试完善

**目标文件**: `src/utils/imageUtils.ts`
**测试文件**: `src/utils/__tests__/imageUtils.test.ts`

**当前覆盖率**: 20.2%
**目标覆盖率**: 80%+

**测试重点**:

- 图像格式转换
- 尺寸调整算法
- 质量压缩
- 元数据处理

**预期覆盖率提升**: +7%

### 阶段五：覆盖率阈值配置和CI集成 (优先级: 中)

#### 5.1 配置覆盖率阈值

**修改文件**: `vitest.config.ts`

```typescript
export default defineConfig({
  test: {
    // ... 现有配置
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html', 'lcov'],
      thresholds: {
        global: {
          statements: 50,
          branches: 50,
          functions: 50,
          lines: 50,
        },
        // 核心模块更高要求
        'src/store/**': {
          statements: 90,
          branches: 80,
          functions: 90,
          lines: 90,
        },
        'src/hooks/**': {
          statements: 80,
          branches: 70,
          functions: 80,
          lines: 80,
        },
        'src/utils/**': {
          statements: 70,
          branches: 60,
          functions: 70,
          lines: 70,
        },
      },
      // 排除不需要测试的文件
      exclude: [
        'src/main.tsx',
        'src/vite-env.d.ts',
        '**/*.d.ts',
        '**/node_modules/**',
      ],
    },
  },
});
```

#### 5.2 更新package.json脚本

```json
{
  "scripts": {
    "test:coverage": "vitest run --coverage",
    "test:coverage:check": "vitest run --coverage --coverage.thresholds.autoUpdate",
    "test:coverage:watch": "vitest --coverage --watch",
    "test:coverage:ui": "vitest --coverage --ui"
  }
}
```

#### 5.3 CI/CD配置更新

**创建文件**: `.github/workflows/test-coverage.yml`

```yaml
name: Test Coverage

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  coverage:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Run tests with coverage
        run: pnpm test:coverage

      - name: Check coverage thresholds
        run: pnpm test:coverage:check

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: true
```

#### 5.4 更新pre-commit钩子

**修改文件**: `package.json`

```json
{
  "scripts": {
    "precommit": "pnpm type-check && pnpm lint && pnpm test:changed && pnpm test:coverage:check"
  }
}
```

## 📅 实施时间表

### 第1周：UI组件测试

- [ ] Header组件测试 (1天)
- [ ] Footer组件测试 (1天)
- [ ] Toolbar组件测试 (2天)
- [ ] ParamsPanel组件测试 (1天)

### 第2周：核心业务逻辑测试

- [ ] imageWorker.ts测试 (3天)
- [ ] imageCollageUtils.ts测试 (2天)

### 第3周：集成测试和工具函数

- [ ] App.tsx集成测试 (2天)
- [ ] filters.ts测试完善 (1.5天)
- [ ] imageUtils.ts测试完善 (1.5天)

### 第4周：配置和优化

- [ ] 覆盖率阈值配置 (1天)
- [ ] CI/CD集成 (1天)
- [ ] 测试优化和重构 (3天)

## 🎯 预期成果

### 覆盖率目标

- **当前**: 20.8%
- **目标**: 55%+
- **核心模块**: 80%+

### 质量指标

- 所有新增测试通过率: 100%
- 测试执行时间: <60秒
- 覆盖率报告生成: <10秒

### 长期收益

- 🔍 提前发现bug，减少生产环境问题
- 🚀 提升开发效率，安全重构代码
- 📈 建立可持续的代码质量保障体系
- 👥 提升团队对代码质量的重视程度

## 📝 测试编写规范

### 命名约定

- 测试文件: `*.test.ts` 或 `*.test.tsx`
- 集成测试: `*.integration.test.ts`
- 测试描述: 使用中文，清晰描述测试意图

### 测试结构

```typescript
describe('组件/功能名称', () => {
  // 测试前准备
  beforeEach(() => {
    // 初始化
  });

  // 测试后清理
  afterEach(() => {
    // 清理
  });

  describe('基础功能', () => {
    it('应该正确渲染', () => {
      // 测试逻辑
    });
  });

  describe('交互功能', () => {
    it('应该响应用户操作', () => {
      // 测试逻辑
    });
  });

  describe('错误处理', () => {
    it('应该正确处理错误情况', () => {
      // 测试逻辑
    });
  });
});
```

### 断言建议

- 使用语义化的断言方法
- 优先使用Testing Library的查询方法
- 避免测试实现细节，关注用户行为

## 🔧 工具和资源

### 测试工具

- **Vitest**: 测试运行器
- **React Testing Library**: 组件测试
- **@testing-library/user-event**: 用户交互模拟
- **@vitest/coverage-v8**: 覆盖率统计

### Mock工具

- **vi.mock()**: 模块Mock
- **vi.fn()**: 函数Mock
- **MSW**: API Mock (如需要)

### 调试工具

- **screen.debug()**: DOM结构调试
- **console.log()**: 调试信息输出
- **vitest --ui**: 可视化测试界面

## 📚 参考资料

- [Vitest官方文档](https://vitest.dev/)
- [React Testing Library文档](https://testing-library.com/docs/react-testing-library/intro/)
- [测试最佳实践](https://kentcdodds.com/blog/common-mistakes-with-react-testing-library)
- [覆盖率配置指南](https://vitest.dev/config/#coverage)

---

**文档版本**: v1.0
**创建日期**: 2024年1月
**更新日期**: 2024年1月
**负责人**: 开发团队
